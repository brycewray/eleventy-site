---
title: BryceWray.com
layout: base
---

  <main>
    <div class="container-home">
      <div class="homeOpen">
        <h1>Welcome&nbsp;to brycewray.com, offering&nbsp;<a href="/about/">its&nbsp;owner</a>&rsquo;s observations, opinions, and&nbsp;geekiness since&nbsp;2018.</h1>
      </div>
      <div class="homePost-Div">
        <div class="post-line"></div>
        <h2 class="homePost-Latest">Latest <a href="/posts/">posts</a></h2>
        {% for post in collections.post | reverse %}
          {% if loop.index0 < 3 %}
          <div>
            <h2 class="homePost-Title"><a href="{{ post.url }}">{{ post.data.title | safe }}</a></h2>
						{% set PublicSans = siteparams.PublicSans %}
						{% set Description = post.data.description %}
						{% if PublicSans == true %}
							{% set Description = post.data.description | replace(" — ", "—") %}
						{% endif %}
            <p class="homePost-Description">
							{{ Description | safe }}
            </p>
            <p class="homePost-Dates">
              {% if not ("The site this week" in post.data.title) %}<strong><time datetime="{{ post.date | dateStringISO }}">{{ post.date | dateStringISO }}</time></strong>{% endif %}
            </p>
          </div>
          {% endif %}
        {% endfor %}
        <p class="homePost-Finish"><a href="/posts/"><strong>All {{ collections.post.length }} posts</strong></a> <span class="pokey"><em>(listed five per page)</em></span></p>
      </div>
    </div>
  </main>
