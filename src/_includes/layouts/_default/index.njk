---
title: BryceWray.com
layout: base
---

  <main>
    <div class="container-home">
      <div class="homeOpen">
        <h1>Welcome&nbsp;to brycewray.com, offering&nbsp;<a href="/about/">its&nbsp;owner</a>&rsquo;s &ldquo;observations, opinions, and&nbsp;geekiness&rdquo; since&nbsp;2018.</h1>
      </div>
      <div class="homePost-Div">
        <div class="post-line"></div>
        <h2 class="homePost-Latest">Latest <a href="/posts/">posts</a></h2>
        {% for post in collections.post | reverse %}
          {% if loop.index0 < 3 %}
          <div>
            <h2 class="homePost-Title"><a href="{{ post.url }}">{{ post.data.title | safe }}</a></h2>
            <p class="homePost-Subtitle">{{ post.data.subtitle | safe }}</p>
            <p class="homePost-Description">
              <em>{{ post.data.description | safe }}</em>
            </p>
            <p class="homePost-Dates">
              {% if not ("The site this week" in post.data.title) %}<strong><time datetime="{{ post.date | dateStringISO }}">{{ post.date | dateStringISO }}</time></strong><br />{% endif %}
              {% if post.data.lastmod and not ((post.date | dateStringISO) == (post.data.lastmod | dateStringISO)) %}
              <span class="pokey">Last modified <time datetime="{{ post.date | dateStringISO }}">{{ post.data.lastmod | dateStringISO }}</time></span>
              {% endif %}
            </p>
          </div>
          {% endif %}
        {% endfor %}
        <p class="homePost-Finish"><a href="/posts/"><strong>All {{ collections.post.length }} posts</strong></a> <span class="pokey"><em>(listed five per page)</em></span></p>
      </div>
    </div>
  </main>
